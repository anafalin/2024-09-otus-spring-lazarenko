server:
  port: 8080

spring:
  data:
    mongodb:
      database: library
      port: 27017
      host: localhost

mongock:
  runner-type: "InitializingBean"
  change-logs-scan-package: ru.otus.hw.changelog
  mongo-db:
    write-concern:
      journal: false
    read-concern: local